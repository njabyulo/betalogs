# ============================================
# AI Services Domain
# ============================================

GOOGLE_GENERATIVE_AI_API_KEY=your_google_ai_api_key_here # Required for AI text generation and embeddings. Get your API key from: https://aistudio.google.com/apikey. The @ai-sdk/google package automatically detects GOOGLE_GENERATIVE_AI_API_KEY. Alternative: GEMINI_API_KEY (if both are set, GOOGLE_GENERATIVE_AI_API_KEY takes precedence)

# ============================================
# Search & Indexing Domain
# ============================================

OPENSEARCH_NODE=http://localhost:9200 # Full URL to your OpenSearch cluster (e.g., https://localhost:9200)
OPENSEARCH_INDEX=activity-events # Base index name for activity events (daily indices will be created automatically)
OPENSEARCH_USERNAME=admin # Username for OpenSearch authentication (optional if no auth)
OPENSEARCH_PASSWORD=admin # Password for OpenSearch authentication (optional if no auth)

# ============================================
# Database Domain
# ============================================

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/betalogs # PostgreSQL Database Configuration. Required for database connection via Drizzle ORM

# ============================================
# Application Domain
# ============================================

# NODE_ENV=development # Is typically set automatically by Next.js. Options: development, production, test
